<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileApp"
        tools:targetApi="31">

        <!-- Attività principale (LoginActivity) -->
        <activity android:name="com.example.mobileapp.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Attività di Registrazione (RegisterActivity) -->
        <activity android:name="com.example.mobileapp.RegisterActivity"
            android:exported="true" />

        <!-- Attività principale dopo il login (MainActivity) -->
        <activity android:name="com.example.mobileapp.MainActivity"
            android:exported="true" />

    </application>

</manifest>



